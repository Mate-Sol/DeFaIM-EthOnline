{
  "name": "defa-subgraph",
  "version": "1.0.0",
  "license": "MIT",
  "description": "Indexes DeFa credit facilities on Arc",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy defa-arc",
    "create-local": "graph create --node http://localhost:8020/ defa-arc",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 defa-arc"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.97.1",
    "@graphprotocol/graph-ts": "0.38.1"
  }
}
